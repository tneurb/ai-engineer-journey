name = "AI Engineer"
languages = ["Python", "SQL", "Bash"]

for lang in languages:
    print(f"Learning {lang} to become an {name}")

numbers = [1, 2, 3, 4, 5, 6]
evens = [n for n in numbers if n % 2 == 0]
print(f"Even numbers: {evens}")